name: tarweeqa_app
description: "نظام ترويقة المتكامل"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  # تم تثبيت الإصدارات لتعمل مع السيرفرات الحالية
  firebase_core: ^3.8.0
  cloud_firestore: ^5.2.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
